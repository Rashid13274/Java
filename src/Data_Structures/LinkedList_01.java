package Data_Structures;



//import org.w3c.dom.Node;

public class LinkedList_01 {
	// all the operation and functionality for linked list  are performed in this class.
	// first node of any linkded list is head so we've created a head.
	/*
	Node head;
	void add(int data) {
		Node toAdd=new Node(data);
		// condition checking if our linked list is empty or not.
		if(head==null) {
			head= new Node(data);
			return;
			
		}
		toAdd.next=null;
		// here we created a temp node for traversal. in linked list if we want to traverse we must need temp node.
		// now  we want to add data or node at the end of linked list. so looping applied.
		// initially it was on head.
		Node temp=head;
		while(temp.next!=null) {
			System.out.println(temp.data+" ");
			temp=temp.next;
		}
		temp.next=toAdd;
	}
	// here we want  to print linked list , so temp node travese to each node and finally at the end whole linked get printed.
	void print() {
		Node temp=head;
		while(temp!=null) {
			System.out.println(temp.data+" ");
			temp=temp.next;
		}
	}
	boolean isEmpty() {
		return head==null;
	}
	// here we've created a node
	static class Node{
		int data;
		Node next;
		public Node(int data) {
			this.data=data;
			next=null;
	// also we want to add node or data  to this linked list.
			*/
	
		
	
	public static void main(String[] args) {
		
		
		
		
	}
		}

	


