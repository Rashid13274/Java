package random_Program;

//import java.util.ArrayDeque;
import java.util.Deque;
import java.util.LinkedList;

public class Deque01 {

	public static void main(String[] args) {
	/* EXAMPLE:1
		Deque<String>dq=new ArrayDeque<>();
		dq.add("apple");
		dq.add("ball");
		dq.add("cat");
		dq.add("elephent");
		System.out.println(dq);  //[apple, ball, cat, elephent]
		System.out.println(dq.poll());
		// remove the elment at  head of the queue.(apple)
	
		System.out.println(dq.pop());
		// remove the element at head. (ball)
		System.out.println(dq.pollFirst());
		// remove the element at end of queue.
		System.out.println(dq.pollLast());
		
		*/
		
		
		/*
		 
		// EXAMPLE:2
		
		Deque<String>dq =new LinkedList<>();
		// we can add element in the queue in the various ways...
		dq.add("first ");
		dq.add("second");
		// add at last
		dq.addLast("three");
		// add first
		dq.push("start");
		// add at end 
		 dq.offer("end");
		 System.out.println(dq);
		 // [start, first , second, three, end]
		 System.out.println(dq.peek());
		 // start
		 System.out.println(dq.peekLast());
		 // end;
		
		*/
	}

}
